// This file is required by the Angular build target referenced by Storybook's
// browserTarget. Storybook bootstraps its own Angular renderer and does not
// use this entry point — stories are compiled via .storybook/tsconfig.json.
console.log('Storybook Angular app entry — not used by Storybook itself.');
